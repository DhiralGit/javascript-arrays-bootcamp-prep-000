var chocolateBars = ['snickers', 'hundred grand', 'kitkat', 'skittles']
chocolateBars.unshift('gums')

var addElementToBeginningOfArray = chocolateBars(array, element) {
  return [element, ...array]
}